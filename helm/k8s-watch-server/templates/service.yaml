apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8s-watch-server.fullname" . }}
  labels:
    {{- include "k8s-watch-server.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k8s-watch-server.selectorLabels" . | nindent 4 }}
